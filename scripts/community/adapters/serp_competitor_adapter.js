const fs=require('fs');const path=require('path');const { buildRawSignal, fetchText, slugify }=require('../signal_utils');
function clean(v){return String(v||'').replace(/<script[\s\S]*?<\/script>/gi,' ').replace(/<style[\s\S]*?<\/style>/gi,' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();}
function readObs(){try{return JSON.parse(fs.readFileSync(path.resolve(process.cwd(),'data/search/query_observations.json'),'utf8'))}catch{return {observations:[]}}}
async function collect(source){const obs=readObs();if(!obs.observations?.length)return {status:'skipped_not_configured',rows:[],error:'No live query observations are available yet.'};const urls=[];for(const o of obs.observations||[])for(const c of o.citations||[]){try{const u=new URL(c.url);if(!u.hostname.endsWith('horselegalguide.com')&&!urls.includes(c.url))urls.push(c.url)}catch{}}const rows=[];for(const url of urls.slice(0,8)){try{const html=await fetchText(url);const h=[...html.matchAll(/<h[1-3][^>]*>([\s\S]*?)<\/h[1-3]>/gi)].map(m=>clean(m[1])).filter(x=>x.length>=12&&x.length<=220);for(const title of h.slice(0,10)){const signal=buildRawSignal(source,{title,source_url:url,short_excerpt:title},rows.length);if(signal){signal.evidence_class='competitor_page_structure';signal.legal_authority=false;rows.push(signal)}}}catch(err){console.warn(`[serp_competitor_adapter] failed for ${url}: ${err.message}`)}}return {status:rows.length?'success_with_data':'success_empty',rows};}
module.exports={collect};
