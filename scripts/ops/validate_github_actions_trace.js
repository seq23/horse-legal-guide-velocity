const fs=require('fs');const path=require('path');function fail(m){console.error('GITHUB_ACTIONS_TRACE_FAIL: '+m);process.exitCode=1;}const p='data/admin/github_actions_trace.json';if(!fs.existsSync(p))fail(`${p} missing. Run npm run ops:simulate-github-actions.`);else{const x=JSON.parse(fs.readFileSync(p,'utf8'));if(!String(x.truth_boundary||'').includes('local simulated'))fail('truth boundary must say local simulated');if(!Array.isArray(x.workflows)||x.workflow_count<8)fail('must trace all expected workflows');if(x.failed>0)fail(`${x.failed} simulated workflows failed`);for(const w of x.workflows||[]){if(!w.workflow||!w.status||!w.report)fail(`malformed workflow row: ${JSON.stringify(w)}`);if(!fs.existsSync(w.report))fail(`missing report ${w.report}`);}}
if(!process.exitCode)console.log('GitHub Actions simulation trace OK');
