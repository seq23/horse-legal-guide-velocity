const fs=require('fs');const path=require('path');
function read(r,f){try{return JSON.parse(fs.readFileSync(path.resolve(process.cwd(),r),'utf8'));}catch{return f;}}function write(r,v){const p=path.resolve(process.cwd(),r);fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,JSON.stringify(v,null,2)+'\n');}function esc(v){return String(v||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');}
function manifest(route){const idx=read('.build/page-manifests/manifest-index.json',{manifests:[]});return (idx.manifests||[]).find(x=>x.slug===route)||null;}function zoneInner(html,name){const m=String(html||'').match(new RegExp(`<section[^>]*data-editable-zone=["']${name}["'][^>]*>([\\s\\S]*?)<\\/section>`,'i'));return m?.[1]?.trim()||'';}function visible(html){return String(html||'').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim();}
function buildPatch(item,revision=1){const m=manifest(item.target_page);if(!m||!fs.existsSync(path.resolve(process.cwd(),m.filePath)))return {status:'unavailable',reason:'page_manifest_or_rendered_target_missing'};const html=fs.readFileSync(path.resolve(process.cwd(),m.filePath),'utf8');const quick=zoneInner(html,'quick_answer_block');if(!quick)return {status:'unavailable',reason:'quick_answer_block_missing'};const allowed=new Set((m.editable_zones||[]).map(z=>z.name));const preferred=revision===1?['faq_block','top_answer_module']:['top_answer_module','related_links_block'];const zone=preferred.find(z=>allowed.has(z));if(!zone)return {status:'unavailable',reason:'no_safe_editable_zone'};const query=esc(item.query);const excerpt=quick.length>3000?`<p>${esc(visible(quick).slice(0,900))}</p>`:quick;const label=revision===1?'Search question':'Search answer reinforcement';const addition=`<div class="query-alignment-repair" data-query-panel="${esc(item.panel_id)}" data-revision="${revision}"><p><strong>${label}:</strong> ${query}</p>${excerpt}</div>`;return {status:'prepared',plan:{mode:'page',slug:item.target_page,operations:[{zone,action:'append_html',html:addition}]},source_reuse:'Uses only already-rendered approved target-page answer content; competitor snippets are not copied or treated as legal authority.'};}
function main(){const d=read('data/search/query_diagnostics.json',{items:[]}),previous=read('data/remediation/query_repair_queue.json',{repairs:[]});const byPanel=new Map();for(const r of previous.repairs||[]){if(!byPanel.has(r.panel_id))byPanel.set(r.panel_id,[]);byPanel.get(r.panel_id).push(r);}for(const rows of byPanel.values())rows.sort((a,b)=>(a.revision||1)-(b.revision||1));const repairs=[];const maxRevisions=2;
 for(const item of d.items||[]){const history=byPanel.get(item.panel_id)||[];const latest=history.at(-1)||null;
   // Preserve historical repair records for durable receipts.
   for(const old of history)repairs.push({...old});
   if(latest?.applied_at){const latestIndex=repairs.findIndex(r=>r.repair_id===latest.repair_id);const observed=item.state==='OBSERVED';if(latestIndex>=0){repairs[latestIndex].diagnosis_state=item.state;repairs[latestIndex].diagnosis=item.reasons;repairs[latestIndex].evidence=item.evidence;repairs[latestIndex].last_retested_at=new Date().toISOString();repairs[latestIndex].state=observed?'observed_after_repair':((latest.revision||1)>=maxRevisions?'bounded_repair_exhausted':'retest_failed');repairs[latestIndex].retest_status=observed?'observed_after_repair':((latest.revision||1)>=maxRevisions?'still_missing_after_bounded_repairs':'still_missing_after_repair');}
     if(observed||!['MISSING','WRONG_PAGE','CANNIBALIZATION'].includes(item.state)||(latest.revision||1)>=maxRevisions)continue;
     const revision=(latest.revision||1)+1,prepared=buildPatch(item,revision),id=`query-repair-${item.panel_id}-r${revision}`;repairs.push({repair_id:id,panel_id:item.panel_id,revision,query:item.query,target_page:item.target_page,diagnosis_state:item.state,diagnosis:item.reasons,evidence:item.evidence,competitor_urls:item.evidence?.competitor_urls||[],state:'prepared_for_owner_review',prepared_patch_status:prepared.status,patch_plan:prepared.plan||null,patch_note:prepared.source_reuse||prepared.reason,approval_required:true,automatic_live_change:false,owner_approved:false,approved_by:null,approved_at:null,rejected_at:null,applied_at:null,retest_status:'not_deployed',prior_repair_id:latest.repair_id});continue;
   }
   if(latest&&!latest.applied_at)continue; // existing pending/rejected/approved proposal remains authoritative until owner action.
   if(!['MISSING','WRONG_PAGE','CANNIBALIZATION'].includes(item.state))continue;const revision=1,prepared=buildPatch(item,revision),id=`query-repair-${item.panel_id}`;repairs.push({repair_id:id,panel_id:item.panel_id,revision,query:item.query,target_page:item.target_page,diagnosis_state:item.state,diagnosis:item.reasons,evidence:item.evidence,competitor_urls:item.evidence?.competitor_urls||[],state:'prepared_for_owner_review',prepared_patch_status:prepared.status,patch_plan:prepared.plan||null,patch_note:prepared.source_reuse||prepared.reason,approval_required:true,automatic_live_change:false,owner_approved:false,approved_by:null,approved_at:null,rejected_at:null,applied_at:null,retest_status:'not_deployed'});
 }
 const uniq=[...new Map(repairs.map(r=>[r.repair_id,r])).values()];const payload={schema_version:'2.2.0',generated_at:new Date().toISOString(),bounded_repair_revisions:maxRevisions,policy:'The system performs query testing, diagnosis, competitor comparison, finished patch preparation, and post-deployment retesting. Client approval remains required before each live legal-page mutation. Bounded on-page repair stops after two revisions.',summary:{tracked:uniq.length,patch_ready:uniq.filter(x=>x.patch_plan&&!x.applied_at).length,owner_approved:uniq.filter(x=>x.owner_approved&&!x.applied_at).length,applied_pending_retest:uniq.filter(x=>x.applied_at&&x.retest_status==='pending_external_retest').length,observed_after_repair:uniq.filter(x=>x.retest_status==='observed_after_repair').length,still_missing_after_repair:uniq.filter(x=>['still_missing_after_repair','still_missing_after_bounded_repairs'].includes(x.retest_status)).length,bounded_repair_exhausted:uniq.filter(x=>x.state==='bounded_repair_exhausted').length},repairs:uniq};write('data/remediation/query_repair_queue.json',payload);write('reports/remediation/query_repair_queue.json',payload);console.log(`Prepared/tracked ${uniq.length} query-driven owner-review repair revisions.`);return payload;}
if(require.main===module)main();module.exports={main};
