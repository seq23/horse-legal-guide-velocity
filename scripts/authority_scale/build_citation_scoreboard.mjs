#!/usr/bin/env node
import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('data/authority_scale/kpi_truth_contract.json','utf8')),l=JSON.parse(fs.readFileSync('data/authority_scale/observed_surfacing_ledger.json','utf8')),idx=JSON.parse(fs.readFileSync('data/authority_scale/fanout_100k/index.json','utf8')),events=l.events||[],counts={};for(const e of events)counts[e.metric]=(counts[e.metric]||0)+1;const verified=counts.verified_citation||0;const out={schema_version:'1.0',generated_at:'2026-07-24T00:00:00.000Z',target_verified_external_citations:c.target.verified_external_citations,target_window_days:c.target.window_days,guarantee:false,fanout_opportunities:idx.materialized_reference_runway,observed_counts:counts,verified_external_citations:verified,remaining_to_target:Math.max(0,c.target.verified_external_citations-verified),truth_note:'Fanout, publishing, indexing, visibility, LLM surfacing, backlinks, and verified citations are separate metrics.'};fs.writeFileSync('data/authority_scale/citation_scoreboard.json',JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));
