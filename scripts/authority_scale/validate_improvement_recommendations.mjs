#!/usr/bin/env node
import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('data/authority_scale/improvement_recommendations.json','utf8')),e=[];if(x.twin_agent_installed!==false)e.push('twin_scope');if(x.policy.includes('automatic editorial mutation')===false)e.push('policy');for(const i of x.items||[]){if(i.editorial_action!=='RECOMMENDATION_ONLY_EXISTING_ADMIN_APPROVAL_REQUIRED')e.push(`authority:${i.route}`);if(!Array.isArray(i.twin_derived_quality_lessons)||i.twin_derived_quality_lessons.length<5)e.push(`lessons:${i.route}`);}console.log(JSON.stringify({ok:!e.length,recommendations:(x.items||[]).length,twin_agent_installed:x.twin_agent_installed,errors:e.slice(0,20)},null,2));if(e.length)process.exit(1);
