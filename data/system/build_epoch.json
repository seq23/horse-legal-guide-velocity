{
  "schema_version": "1.0.0",
  "fallback_build_timestamp": "2026-08-08T01:20:00.000Z",
  "purpose": "Deterministic clean-room fallback when git metadata is unavailable. Git-backed builds prefer the HEAD commit timestamp unless BUILD_TIMESTAMP is explicitly supplied."
}
