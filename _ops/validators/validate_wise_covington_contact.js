const fs=require('fs');function fail(m){console.error('WISE_COVINGTON_CONTACT_FAIL: '+m);process.exitCode=1}const p='data/firm/wise_covington_contact.json';if(!fs.existsSync(p))fail(`${p} missing`);else{const x=JSON.parse(fs.readFileSync(p,'utf8'));if(x.firm_name!=='Wise Covington PLLC')fail('firm name must be Wise Covington PLLC');if(!String(x.canonical_site||'').startsWith('https://wisecovington.com'))fail('canonical site must be wisecovington.com');const names=(x.attorneys||[]).map(a=>a.name);for(const n of ['Andrea Benavides Wise','Claire Covington'])if(!names.includes(n))fail(`missing attorney from canonical source: ${n}`);}if(!process.exitCode)console.log('Wise Covington contact OK');
