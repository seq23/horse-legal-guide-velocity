const fs=require('fs');function fail(m){console.error('CONTACT_COVERAGE_FAIL: '+m);process.exitCode=1}const p='data/admin/seo_dashboard.json';if(!fs.existsSync(p))fail(`${p} missing`);else{const x=JSON.parse(fs.readFileSync(p,'utf8'));if(typeof (x.health||{}).routing_contact!=='number')fail('routing_contact health score missing');}if(!process.exitCode)console.log('Contact coverage OK');
