const fs=require('fs');function fail(m){console.error('SELF_HEAL_REPORT_FAIL: '+m);process.exitCode=1}const p='data/admin/self_heal_report.json';if(!fs.existsSync(p))fail(`${p} missing`);else{const x=JSON.parse(fs.readFileSync(p,'utf8'));if(!Array.isArray(x.results))fail('results missing');if(x.failed>0)fail(`${x.failed} self-heal failures remain`)}if(!process.exitCode)console.log('Self-heal report OK');
