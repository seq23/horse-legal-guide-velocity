const fs=require('fs');function fail(m){console.error('SCHEMA_COMPLETENESS_FAIL: '+m);process.exitCode=1}const p='data/admin/schema_audit.json';if(!fs.existsSync(p))fail(`${p} missing`);else{const x=JSON.parse(fs.readFileSync(p,'utf8'));if(!Array.isArray(x.recommended_schema_types))fail('recommended schema types missing');}if(!process.exitCode)console.log('Schema completeness report OK');
