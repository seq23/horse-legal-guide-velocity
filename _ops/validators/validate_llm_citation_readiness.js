const fs=require('fs');function fail(m){console.error('LLM_CITATION_READINESS_FAIL: '+m);process.exitCode=1}const p='data/admin/seo_dashboard.json';if(!fs.existsSync(p))fail(`${p} missing`);else{const x=JSON.parse(fs.readFileSync(p,'utf8'));for(const k of ['citation_velocity','content_atom_coverage','routing_contact'])if(typeof (x.health||{})[k]!=='number')fail(`missing ${k} score`);if(!String(x.purpose||'').toLowerCase().includes('citation'))fail('dashboard purpose must mention citation');}if(!process.exitCode)console.log('LLM citation readiness OK');
