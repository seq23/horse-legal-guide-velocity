# Admin Panel Runbook

Superseded by `RUNBOOK.md` at the repo root — this repo's single operator runbook. This file previously described the admin panel directly; that content drifted out of sync with the actual `/admin/` page (it referenced per-card "copy approve command" buttons and individual GitHub edit links that no longer exist) and is retired here rather than left to disagree with reality.

See `RUNBOOK.md` for how to approve, reject, or send back a draft from `/admin/`, what happens automatically afterward, and how to confirm it actually went live.
